The next password is stored in the file data.txt, which contains base64 encoded data

In this assignment the content of data.txt is base64 encoded. To solve this we can go two routes:

1: base64 --decode data.txt
2: cat data.txt | base64 --decode

We get the same output:

The password is PASSWORD
